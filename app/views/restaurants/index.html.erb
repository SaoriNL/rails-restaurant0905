<div class= "container">
  <h1>Restaurants</h1>
  <% @restaurants.each do |r| %>
    <%= link_to restaurant_path(r) do %>
      <h3><%= r.name %></h3>
    <% end %>
    <h3><%= r.address %></h3>
    <h3><%= r.rating %></h3>
  <% end %>
</div>
